## Signin
```
const bridge = await BitfiBridge.signin('NOX_URL', {
  deviceId: 'DEVICE_ID',  
  apiUrl: 'API_URL',
  envoyUrl: 'ENVOY_URL'
})
```

## Get accounts
```
const accounts = await bridge.getAccounts()
```
